{
  "name": "reai-monorepo",
  "version": "1.0.0",
  "description": "REAI Monorepo",
  "scripts": {
    "install:all": "npm install && cd frontend && npm install && cd ../web && npm install",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:web\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && .venv\\Scripts\\python.exe -m uvicorn app.main:app --reload",
    "dev:web": "cd web && npm run dev",
    "dev:frontend": "cd frontend && npm run dev:electron"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
